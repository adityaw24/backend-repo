specVersion: "v1beta"
serviceId: "backend-repo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "backend-repo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
